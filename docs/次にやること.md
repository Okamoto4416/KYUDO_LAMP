
# やりたいこと

- NetworkMngr解体
    - 複雑すぎるので。
    - LinkMonitorを簡単に
    - stateもAP接続だけでいい気がする
    - 省電力モードの無効化
- ネットワーク ESP-NOWをテスト
    - APなし
    - 併用
- 同期
    - 双方が合わせる必要あんまりないよな。
    - 隔離したら改造
    - ランプ側を親機にして動作させればいいと思う
        - 子機のリモコン側のみが推定
- 状態共有ランプの実装(remote側)
    - LED 1:ランプの状態
    - LED2:送信の状態？
        - 送信試行状態の時は点灯